package frc.robot.subsystems.arm;

import org.ejml.simple.SimpleMatrix;

import frc.robot.utils.Vector2;

public class ArmLink {
    // // constant fields
    // private final double mass;
    // private final double length;
    // private final Vector2 comOffset;
    // private final Vector2 jointOffset;
    // private final double moi;
    // private static final double g = -386.0885826772;
    // private final ArmLink prevLink;
    // private final boolean firstLink;

    // public ArmLink(){
    //     this.mass = mass;
    // }

    // //transforms a 
    // public SimpleMatrix calcTransformMatrix(double theta){
    //     SimpleMatrix ret = new SimpleMatrix(new double[][]{
    //         {Math.cos(theta), -Math.sin(theta), 0, jointOffset.dot(new Vector2(Math.cos(theta), Math.sin(theta)))},
    //         {Math.sin(theta), -Math.cos(theta), 0, jointOffset.dot(new Vector2(-Math.sin(theta), Math.cos(theta)))},
    //         {0,0,1,theta},
    //         {0,0,0,1}
    //     });
    //     return ret;
    // }

}
